apiVersion: v2
name: rscounter
description: Rust counter service — owns the Postgres database
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: common
    version: 0.1.0
    repository: file://../common
