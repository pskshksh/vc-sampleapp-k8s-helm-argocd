apiVersion: v2
name: goapi
description: Go HTTP API — the only service the frontend talks to
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: common
    version: 0.1.0
    repository: file://../common
